import BridgeWaterScene from "@/components/BridgeWaterScene";

const Index = () => {
  return (
    <div className="relative h-screen w-full overflow-hidden bg-[#08090d]">
      <section className="relative h-full w-full">
        <BridgeWaterScene />
      </section>
    </div>
  );
};

export default Index;
